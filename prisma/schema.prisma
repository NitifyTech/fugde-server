datasource db {
  provider = "mongodb"
  url      = env("MONGO_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Especies {
  id String @id @default(auto()) @map("_id") @db.ObjectId
}
